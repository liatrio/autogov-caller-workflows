name: Build Image Caller Workflow

on:
  workflow_dispatch:
  push:
    branches: ['**']
    paths-ignore:
      - .github/workflows/wf-slack-alert.yaml
      - README.md
      - renovate.json
  pull_request:
    paths-ignore:
      - .github/workflows/wf-slack-alert.yaml
      - README.md
      - renovate.json

concurrency:
  # serialize main pushes so back-to-back releases don't race the cut;
  # never cancel a run mid-publish (rw-release isn't idempotent-resume)
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

jobs:
  build-image:
    permissions:
      actions: read
      id-token: write
      attestations: write
      packages: write
      contents: write
      pull-requests: read
      artifact-metadata: write
    uses: liatrio/autogov-workflows/.github/workflows/rw-build-image.yaml@69ae7bdc931567721161895d6245a27fbab2cd06 # v1.1.5
    secrets: inherit
    with:
      registry: ghcr.io
      cert-identity: https://github.com/liatrio/autogov-workflows/.github/workflows/rw-attest-image.yaml@69ae7bdc931567721161895d6245a27fbab2cd06 # v1.1.5
      # keep the example run advisory so a demo FAILED VSA is surfaced without
      # breaking the run; the FAILED VSA is still attested and uploaded.
      allow-failed-vsa: true
